name: Update JIRA
on:
  pull_request:
    types:
      - opened
      - synchronize
      - closed
env:
  JIRA_USERNAME: ${{ secrets.JIRA_USERNAME }}
  JIRA_PASSWORD: ${{ secrets.JIRA_PASSWORD }}
  CONFLUENCE_URL: ${{ secrets.CONFLUENCE_URL }}
  
jobs:
  update_jira_confluence:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: installing jq
        run: sudo apt-get install jq
      - name : Get Pull Request Details
        run: |
          PR_TITLE=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                 https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }} | \
                 jq -r '.title')
          PR_DESCRIPTION=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                       https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }} | \
                       jq -r '.body')
        env:
          PR_TITLE: ${{ env.PR_TITLE }}
          PR_DESCRIPTION: ${{ env.PR_DESCRIPTION }}
      - name: Push Data to Confluence
        run: |
          PAGE_ID="393217"
          TABLE_CONTENT="||Title||Description||\n|${{ env.PR_TITLE }}|${{ env.PR_DESCRIPTION }}|"

          curl -s -u jadhavabhijeet6411@gmail.com:ATATT3xFfGF0Q3evy1tGz9XlYXsxZ66CKlkq3tCqd6K6W1CtT87mB6v4Z9jHV3FqSMGYSnapR25gdtG0JQEaBOr4MhUrFC2RKIFNzy5mHdPpJSgjn3X95gk-gN3b6urTtXULgir1hjEqeHDky-dOY6dkHS-R8U__b-CEok9oPHjVWKLYR7KGbLA=90C2A2DB \
            -X PUT \
            -H "Content-Type: application/json" \
            -d '{
               "version": {
                 "number": 1
                  },
               "title": "PR requests",
               "type": "page",
               "body": {
                 "storage": {
                 "value": "<table>${TABLE_CONTENT}</table>",
                 "representation": "storage"
                 }
               }
            }' \
          https://demo-sw.atlassian.net/wiki/rest/api/content/393217
